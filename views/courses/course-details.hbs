<div class='container center-vertical'>
  <div class='row'>
    <div class='col-md-6'>
      <img
        src='{{course.imageUrl}}'
        class='img-fluid mt-4'
        alt='Course Image'
      />
    </div>
    <div class='col-md-6 mt-4'>
      <h1 class='course-details-title'>{{course.title}}</h1>
      <p>{{course.description}}</p>
      <p>Instructor: {{course.instructor}}</p>
      {{!-- <h1>{{currentUser.accountType.isStudent}}</h1> --}}
      {{#if currentUser.accountType.isStudent}}
        <form action='/courses/{{course._id}}/enroll' method='POST'>
          <button type='submit' class='btn btn-primary'>Enroll</button>
        </form>
      {{else}}
        <a href='/courses/{{course._id}}/edit' class='btn btn-purple'>Edit</a>
        <a
          href='/courses/{{course._id}}/delete'
          class='btn btn-danger'
        >Delete</a>
        <a class='btn btn-primary' href='/instructor/dashboard'>Back</a>

      {{/if}}
    </div>
  </div>
</div>

{{!-- <h1>Course Details</h1>
<div>
  <img src='{{imageUrl}}' alt='Course Image' />
  <h2>{{title}}</h2>
  <p>Description: {{description}}</p>
  <p>Instructor: {{instructor}}</p>

</div>

{{#if currentUser.isStudent}}
  <form action='/courses/{{_id}}/enroll' method='POST'>
    <button type='submit'>Enroll in Course</button>
  </form>
{{/if}}

<a href='/courses/create'>Create courses</a>

<form action='/courses/{{course._id}}/delete'>
  <button>Delete</button>
</form>

<a href='/courses/{{_id}}/edit'>Edit Course</a>

<!-- Enrollment button -->
<form action='/courses/{{_id}}/enroll' method='POST'>
  <button type='submit'>Enroll</button>
</form> --}}

{{!-- <h1>Course Details</h1>
<div>
  <h2>{{title}}</h2>
  <p>Description: {{description}}</p>
  <p>Instructor: {{instructor}}</p>
  <p>Rating: {{rating}}</p>
  <p>Lessons: {{lessons}}</p>
</div>

{{#if currentUser.isStudent}}
  <form action='/courses/{{_id}}/enroll' method='POST'>
    <button type='submit'>Enroll in Course</button>
  </form>
{{/if}}

<a href='/courses/create'>Create courses</a>

<form action='/courses/{{_id}}/delete'>
  <button>Delete</button>
</form>

<a href='/courses/{{_id}}/edit'>Edit Course</a>

<!-- Enrollment button -->
<form action='/courses/{{_id}}/enroll' method='POST'>
  <button type='submit'>Enroll</button>
</form> --}}